declare -x fact
declare -x name
declare -x value

for fact in $facts; do
    name="${fact}_uc"
    value="$(printenv "$fact" | tr '[:lower:]' '[:upper:]')"
    declare -xr $name="$value"
    transforms="$transforms $name"
done
unset fact name value
