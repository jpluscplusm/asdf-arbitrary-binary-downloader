declare -x fact
declare -x name
declare -x value

for fact in $facts; do
    name="${fact}_lc"
    value="$(printenv "$fact" | tr '[:upper:]' '[:lower:]')"
    declare -xr $name="$value"
    transforms="$transforms $name"
done
unset fact name value
